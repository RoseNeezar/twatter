import React, { FC } from "react";

const MessageContent: FC = () => {
  return (
    <div className="relative flex w-full h-full overflow-hidden justify-items-start"></div>
  );
};

export default MessageContent;
